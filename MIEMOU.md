# 总则

为当前仓库创建或更新 `AGENTS.md` 文件。

目标：生成一份精简的指令文件，帮助Agent后续会话规避错误、快速上手。每一条内容都要回答这个问题：「如果没有这份提示，AI Agent 是否很容易遗漏该信息？」，如果答案是否定的，就不要写入。

必须遵守用户给出的重点与约束。

## Skill 安装

提问用户是否安装以下skill，需告知用户每个skill的功能

### skill 安装清单

- **karpathy-guidelines** — 行为准则，减少 LLM 常见编码失误（过度工程、臆测假设、缺乏验证标准），适用于所有代码编写、审查、重构场景
- **ponytail** — 强制最简可行方案，抑制过度工程，适用于所有编码任务：编写、添加、重构、修复、审查或设计代码，以及选择库或依赖项时
- **diagnosing-bugs** — 困难 bug 与性能回退的诊断循环，适用于用户报告功能异常、抛出异常、运行缓慢或明确要求诊断时
- **improve-codebase-architecture** — 扫描代码库的深度优化机会，生成可视化报告并逐项审视，适用于架构优化、模块接口改进、可测试性提升场景
- **grill-with-docs** — 对计划或设计进行高压审视，同时产出 ADR 和术语表文档，适用于需要同时验证决策和沉淀文档的场景
- **tdd** — 测试驱动开发，红-绿-重构循环，适用于团队要求测试先行或需要编写集成测试的场景

### 安装原则

- 不安装与项目技术栈无关的 skill
- 不安装功能重叠的 skill（如 ponytail 与 ponytail-review 同时默认安装）
- 安装失败不影响整体初始化流程，跳过即可

## 规则安装

从 <https://github.com/multica-ai/andrej-karpathy-skills> 仓库获取 `CLAUDE.md` 文件，保存到当前项目根目录。

获取地址：`https://raw.githubusercontent.com/multica-ai/andrej-karpathy-skills/main/CLAUDE.md`

该文件为编码行为准则，适用于所有代码编写场景。如果项目根目录已存在 `CLAUDE.md`，将获取内容与现有内容合并，保留项目专属指令，去重冲突条款。

## Agents 文档

### 调研方式

优先阅读高价值信息源：

- `README*`、根目录清单文件、工作区配置、锁文件
- 构建、测试、代码检查、格式化、类型检查、代码生成相关配置
- CI工作流、pre‑commit / 任务运行器配置
- 现有指令文件（`AGENTS.md`、`CLAUDE.md`、`.cursor/rules/`、`.cursorrules`）

阅读完配置与文档后，如果仍不清楚整体架构，需要查看少量具有代表性的代码文件，找出真实入口、包边界以及执行流程。优先阅读能够说明系统整体串联逻辑的文件，不要随意翻看底层零散文件。

优先采信**可执行的真实源文件**，而非纯文字文档。如果文档和配置/脚本存在冲突，以可执行源文件为准，仅保留可以验证的内容。

### 需要提取的内容

提取Agent在本仓库工作时最高价值的关键信息：

- 精确的开发者命令，尤其是不容易直观想到的命令
- 如何运行单个测试、单个包、或是局部验证步骤
- 有强制顺序要求的命令（例如 `lint -> typecheck -> test`）
- 单体多包仓库边界、核心目录归属、真实应用/库入口
- 框架/工具链特殊行为：生成代码、数据库迁移、代码生成产物、构建输出文件、特殊环境变量加载逻辑、开发服务器、基础设施部署流程
- 本仓库独有的编码风格、工作流约定（和框架默认行为不一样的部分）
- 测试相关特殊点：测试固件、集成测试前置依赖、快照处理流程、依赖的外部服务、不稳定/耗时长的测试套件
- 现有指令文件中值得保留的重要约束

一份质量好的 `AGENTS.md`，内容通常是需要通读多个文件才能推导出来的隐性上下文。

### 提问规则

仅当仓库本身无法解答关键信息时，才向用户提问。使用 question 工具，一次最多提交一批简短问题。

适合提问的场景：

- 没有文档记录的团队内部约定
- 分支 / PR / 版本发布相关要求
- 已知但没有书面记录的环境搭建、测试前置条件

仓库已经写清楚的内容，不要重复提问。

### 编写规则

只包含高价值、本仓库专属指引：

- Agent很容易猜错的精确命令与快捷操作
- 仅看文件名无法得知的架构说明
- 不同于语言/框架默认行为的约定
- 环境搭建要求、环境特殊行为、运行时坑点
- 重要的现有指令文件引用

需要排除的内容：

- 通用软件开发常识
- 长篇教程、完整目录树罗列
- 语言本身显而易见的通用规范
- 猜测性内容、无法验证的描述

拿不准要不要写，就直接省略。

优先使用简短章节 + 项目符号列表。如果仓库简单，文件就保持简洁；如果仓库庞大，只总结会真正改变Agent工作方式的核心架构事实。

如果项目路径下已经存在 `AGENTS.md`，在原文件基础上迭代优化，不要盲目全盘重写。保留经过验证的有效指引，删除冗余、过时内容，使其和当前代码库保持一致。
